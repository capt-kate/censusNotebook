services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: census_notebook
      POSTGRES_USER: census
      POSTGRES_PASSWORD: census
    ports:
      - "5432:5432"
    volumes:
      - census_postgres_data:/var/lib/postgresql/data

volumes:
  census_postgres_data:
